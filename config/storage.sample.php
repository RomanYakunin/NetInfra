<?php
/**
 * config/storage.php — где хранятся сканы накладных и расписок.
 *
 * Файл в .gitignore: путь к сетевой папке предприятия в репозиторий не
 * попадает. Этот образец — с пустыми значениями.
 * Меняется страницей «Телефоны» → «⚙ Хранилище» либо вручную здесь.
 *
 * Раскладка внутри корня:
 *     <docs_root>/<Подразделение>/<год>/<файл>
 * Папки подразделений и годов создаются сами при первом сохранении —
 * корень достаточно оставить пустым.
 */

return [
    // UNC-путь к сетевой папке либо локальный каталог.
    // Пример: '\\\\fileserver\\share\\Документы связи'
    'docs_root' => '',

    // Раскладывать по папкам подразделений. Если выключено, всё
    // складывается в <docs_root>/<год>/
    'use_departments' => true,

    // Подразделение неизвестно — папка для таких документов
    'unsorted_folder' => 'Без подразделения',

    // ---------------------- Сканирование ----------------------
    // Работает через WIA. Требует расширения com_dotnet в php.ini
    // (php_com_dotnet.dll входит в поставку OSPanel, его нужно только
    // раскомментировать) и сканера, видимого машине с NetInfra.
    'scan_enabled' => true,

    // Разрешение и формат по умолчанию
    'scan_dpi'    => 300,
    'scan_format' => 'jpeg',   // jpeg | png | tiff | bmp
    'scan_color'  => 'color',  // color | gray | bw
];
