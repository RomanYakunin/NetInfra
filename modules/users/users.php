<?php
// modules/users/users.php — подготовка данных страницы «Пользователи».
// Страница доступна только администратору (проверка дублируется в index.php).
require_once __DIR__ . '/../../includes/acl.php';

if (!isAdmin()) {
    $error = 'Доступ запрещён: страница доступна только администратору';
    return;
}

// Роли для селектов формы
$userRoles = [
    'admin' => 'Администратор',
    'user'  => 'Пользователь (только просмотр)',
];
